# Python deps for the orchestrator/verifier (find_pump_keys.py).
# The CUDA engine itself has no Python dependency.
pymongo>=4.6,<5
solders>=0.21          # trusted ed25519 / Solana keypair derivation + verify
base58>=2.1
sshtunnel>=0.4         # optional SSH-tunnel to a private MongoDB host
# paramiko 4.0+ removed DSSKey, which sshtunnel still references during init,
# so pin to the 3.x series until sshtunnel is updated upstream.
paramiko>=3.4,<4
